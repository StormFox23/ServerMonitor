# Development build for Angular 21 client with hot reload
FROM node:20-alpine

# Install dependencies
RUN apk add --no-cache libc6-compat

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm ci

# Copy source code
COPY . .

# Disable Angular CLI analytics for non-interactive environments
RUN npx ng analytics off

# Expose port 4200
EXPOSE 4200

# Start Angular development server with host binding for Docker
CMD ["npm", "run", "start", "--", "--host", "0.0.0.0"]